def my_hash_creator(key, value)
  { key => vlue }
end

def read_from_hash(hash, key)
  has[key]
end

def update_counting_hash(hash key)
  if hash[key]
    hash[key] = hash[key] + 1 
  elsehash[key] = 1 
end
